const bcrypt = require("bcrypt");
const { key, sign } = require("../utils/jwt");

const adminAuth = (req, res) => {};

const setEmailDomain = (req, res) => {};

module.exports = {
  adminAuth,
  setEmailDomain,
};
